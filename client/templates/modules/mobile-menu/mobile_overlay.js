/*Template.mobile_overlay.rendered = function() {
  Session.set('menuActive', false);
  Session.set('mobileGiveaway', false);
}

Template.mobile_overlay.helpers({
  'mobileMenu': function(){
    return Session.get('menuActive') ? 'show' : '';
  }
});


Template['mobile_overlay'].events({
  'click .menu-toggle' : function () {
    Session.set('menuActive', !Session.get('menuActive'));
  }
});

Template.mobile_modal.helpers({
  'mobileGiveaway': function(){
    return Session.get('mobileGiveaway') ? 'show' : '';
  }
});


Template['mobile_modal'].events({
  'click .gift' : function () {
    Session.set('mobileGiveaway', !Session.get('mobileGiveaway'));
  }
});
*/
